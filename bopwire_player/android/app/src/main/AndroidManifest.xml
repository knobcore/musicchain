<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- ConnectivityManager.NetworkCallback in MainActivity uses this to
         notice wifi ↔ cellular flips and force-redial librats so the
         choppy gap is sub-second instead of 30 s. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Legacy storage permissions for Android <= 12. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <!-- Android 13+ replaces READ_EXTERNAL_STORAGE for audio access. -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <!-- Broad file access (Android 11+). User must toggle "All files
         access" in Settings — covers user-picked folders outside the
         Music/ tree. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage"/>
    <!-- workmanager periodic LibraryScanner task + BopwireService
         partial wake lock so the librats handshake survives screen-off. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Doze exemption: the foreground service + wake lock are not enough
         on cellular — Android throttles the Dart isolate ~30 s after the
         screen goes off and librats RX stalls. MainActivity prompts the
         user once to whitelist us from battery optimization. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Foreground service permissions: the swarm-link service uses
         dataSync (we maintain a persistent UDP/TCP link). Android 14+
         requires both the umbrella permission AND the typed permission. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- allowBackup=false: the wallet mnemonic lives in flutter_secure_storage,
         encrypted by a NON-exportable Android Keystore key. Android Auto-Backup
         would copy the encrypted blob to the cloud and restore it on reinstall,
         but the Keystore key never comes back — so the restored blob is
         undecryptable (BadPaddingException) and used to wedge the wallet gate on
         boot. Disabling backup keeps the encrypted blob and its key on the same
         device, so they live and die together. The mnemonic the user wrote down
         is the real recovery path; on-device data is intentionally ephemeral. -->
    <application
        android:label="bopwire_player"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Foreground service that pins the Flutter process so the
             librats client survives screen-off / app-backgrounded. -->
        <service
            android:name=".BopwireService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
